# Plans

This folder contains research and design documents for major changes to Victual. Each
plan describes the problem, current behavior, proposed scope, constraints, alternatives,
open questions, and verification criteria. It provides the information needed to prepare
an implementation plan; it is not a coding prompt or a session-by-session task list.

Read the [constitution](../constitution.md) and [ADR index](../adr/README.md) first, then
use the status table and work order below to find the relevant plan and its dependencies.
Plan numbers are permanent identifiers, not execution order.

## Status

This table is the authority on delivery status, updated through 2026-09-15. “Landed” means
implemented; outstanding verification and follow-up work are listed separately. A plan's
**Executed** section records what shipped and any differences from the proposed design.

| # | Plan | Status | Dependencies or remaining work |
|---|---|---|---|
| — | [PostgreSQL support](../../db/pgsql/README.md) | Landed | SQLite runtime retirement remains. |
| 01 | [Database file storage](01-file-storage.md) | Landed | PostgreSQL; migration 0258. |
| 02 | [MCP endpoint](02-mcp-endpoint.md) | Draft, [issue 86](https://github.com/datagen24/victual/issues/86) | Wave 5. Read the [interface spec](../mcp-interface-spec.md), which supersedes the body. Requires 11, 13, 14 piece 2, and 15-C1. |
| 03 | [Category minimum stock](03-category-min-stock.md) | Landed | Wave 3b; PostgreSQL; migration 0268. Note-only shopping list row (Q1) remains a follow-up. **The nested `parent_product_group_id` follow-on is [30](30-nested-product-groups.md)'s**, landed 2026-09-15; whether a parent group's minimum rolls up to descendant groups is that plan's own Q1, still unanswered, and touches `product_groups_missing`, which this plan shipped and 30's suite phase confirms is unaffected. |
| 04 | [Seed datasets](04-seed-datasets.md) | Draft, unscheduled | Importer when needed; dataset curation is ongoing. |
| 05 | [Store shopping lists](05-store-shopping-lists.md) | Draft, [issue 85](https://github.com/datagen24/victual/issues/85) | 12 landed. Parts A/C in wave 5; B depends on usage. No migration number is claimed yet; claim one before writing part A. |
| 06 | [Location barcodes](06-location-barcodes.md) | Landed; one follow-up | Wave 3b, [issue 79](https://github.com/datagen24/victual/issues/79) **closed 2026-09-09 on physical evidence**: a location label requested, printed on the QL-820NWBc, scanned back by an authorized user, and the failure-and-reprint cycle demonstrated against the device. Print actions on the list and form (PR 113) and the stateless scan surface (PR 108) are in `master`. **Q5's tree path landed 2026-09-15** ([issue 137](https://github.com/datagen24/victual/issues/137)): the print payload's field catalogue and the `/locationlabels` scan page both show the path from `locations_resolved`; see plan 06's [Executed](06-location-barcodes.md#question-5-the-tree-path-2026-09-15) entry. Remaining: the placement convention, which is documentation, not code. Interactive current-location scanning stays deferred to a plan of its own. |
| 07 | [Nested products](07-nested-products.md) | **Retired 2026-09-14** | Q6's sampling (2026-09-13) landed on **taxonomy** — seven of thirteen candidate pairs are separate products and none is a pool — and [ADR-0023](../adr/0023-taxonomy-is-groups-packaging-is-parent-product.md) accepted it 2026-09-14 with all four prerequisites met. Retired by its own pull request after the acceptance, which closed [issue 82](https://github.com/datagen24/victual/issues/82). Replaced by [30](30-nested-product-groups.md) for the tree of kinds and [31](31-directed-substitution.md) for the relation between forms; `hierarchy_depth_limit()` goes to 30, the one-level `parent_product_id` stays for packaging under decision 2, and its `INSERT` enforcement gap, [issue 148](https://github.com/datagen24/victual/issues/148), is fixed — migration `0277.pgsql.sql`. |
| 08 | [Nested locations](08-nested-locations.md) | Landed | Wave 4, [issue 81](https://github.com/datagen24/victual/issues/81). Migration 0273: `locations.parent_location_id`, the `locations_resolved` read entity under `STOCK_VIEW`, `hierarchy_depth_limit()` (which [07](07-nested-products.md) shares), a cycle/depth guard and a delete guard. Raises the engine minimum to PostgreSQL 15 for `NULLS NOT DISTINCT`. Verified by a `locations` suite phase and a `frontend-security` browser probe. [23](23-storage-classes.md) added `storage_class_id` to the same table on 2026-09-14, and its migration's `is_freezer` derivation is checked in this plan's suite phase. |
| 09 | [US barcode lookup sources](09-barcode-lookup-sources.md) | Deferred, [issue 80](https://github.com/datagen24/victual/issues/80) | Q1's kitchen experiment; S14 before adding sources. Both can run now; the plugins wait on the experiment's data. |
| 10 | [Cold start and statelessness](10-cold-start-statelessness.md) | Landed | Pairs with 01 for a runtime without a persistent volume. |
| 11 | [API errors and authentication](11-api-error-handling.md) | Landed with follow-ups | API key expiry/rotation, sweep S11's remaining half, landed 2026-09-15 ([issue 130](https://github.com/datagen24/victual/issues/130)); Q5's schema-derived allowlist still waits on 14 piece 2. The plan carries no Executed section; what shipped is recorded inline under each section. |
| 12 | [Frontend shared core](12-frontend-shared-core.md) | Landed | S29 follow-up fixes and its CI guard landed through 21. |
| 13 | [Write-path transactions](13-write-path-transactions.md) | Landed | See Executed for transaction-nesting behavior. |
| 14 | [Contract and regression scaffolding](14-contract-and-regression-scaffolding.md) | **Landed 2026-09-17** | All four pieces landed. Piece 2, [issue 83](https://github.com/datagen24/victual/issues/83), closed wave 5: the response-contract snapshot, `tests/Pgsql/ContractTest.php` on `PgsqlSchemaTestCase` per ADR-0025 decision 4, the route-table-vs-spec parity assertion, and the two spec fixes it landed with. Not closed by this piece, despite the issue's own text: S15 and S16's remaining half (11's Q5) — see [Executed](14-contract-and-regression-scaffolding.md#executed) for why. |
| 15 | [Cleanup](15-deliberate-cleanup.md) | **Landed 2026-09-17** | C1–C14 and B1, B2 all landed, [issue 132](https://github.com/datagen24/victual/issues/132) closed the remainder (C2, C3 — found already done, C5–C14). C11 is sweep S13. Two items left as documented residuals rather than done: `BaseController::Render()`'s `$_GET['embedded']` (C8, no `Request` reaches it without threading one through ~103 call sites for a non-security-sensitive flag) and unifying `StockService`'s LessQL-row-vs-`stdClass` returns (C10's second half, its own larger undertaking). B3 declined (Q5); B4 does not apply — C7 resolved the PHP floor to 8.4, downward. |
| 16 | [Project rename](16-project-rename.md) | Code rename landed | Registry/domain claims await announcement. |
| 17 | [Ecosystem clients](17-ecosystem-clients.md) | Premise replaced 2026-09-15 | [ADR-0024](../adr/0024-the-fork-writes-its-own-clients.md), accepted 2026-09-15: the fork writes its own clients, so the couplings here are a catalogue for those clients, not breaks to avoid. Q2/Q4 answered; Q1 and part of Q3 become the client repositories' questions. Client implementations are outside this repository's wave order. |
| 18 | [MQTT state publication](18-mqtt-state-publication.md) | Landed | Home Assistant checks 2, the Home Assistant half of 4, and 8 remain and need a running Home Assistant, [issue 139](https://github.com/datagen24/victual/issues/139). Includes InfluxDB events through an outbox. |
| 19 | [Roles and data visibility](19-rbac.md) | Landed; follow-ups open | Wave 3a: roles and six domain read permissions, 2026-09-05; [ADR-0018](../adr/0018-role-grants-and-domain-reads.md) records the model, accepted 2026-09-14. Piece 2 landed 2026-09-15 as migration `0281.pgsql.sql`, [issue 84](https://github.com/datagen24/victual/issues/84): `STOCK_PRICES_VIEW` under `STOCK_PURCHASE`, `permission_fields`, redaction at the API boundary, filter and order refusal. The review's four remaining price channels ([issue 176](https://github.com/datagen24/victual/issues/176) — the importer's `TRUNCATE ... CASCADE`, `/stock/bookings/{id}`, `product_barcodes.last_price`, four flag-only pages) closed the same day as migration `0282.pgsql.php`, which moves the seed into `db/pgsql/prices-seed.sql` so the importer can re-apply it. Both pieces now carry an Executed section. |
| 20 | [Container infrastructure](20-container-infrastructure.md) | Piece 1 and part of 3 landed | Production Docker target retired. Pieces 2, remaining 3, 4, 5 and the credential split remain and are unblocked, [issue 133](https://github.com/datagen24/victual/issues/133); piece 4's K3S apply is the same work as 25's verification 12. SIGTERM verification needs a cluster that honours `lifecycle.stopSignal`. |
| 21 | [Frontend sink discipline](21-frontend-sink-discipline.md) | Landed | CI payload checks and stored-HTML cleanup on upgrade/import included. |
| 22 | [Medication tracking](22-medication-tracking.md) | Draft, unscheduled | 23 landed 2026-09-14, so only 14 piece 2 remains; ADR-0015/0016 remain Proposed. Q6's unresolved ownership of label infrastructure is now 25's. Reservations 0284–0285, moved up from 0275–0276 on 2026-09-14 when the scheduled wave 4 plans took the lower slots, then again the next day when [issue 148](https://github.com/datagen24/victual/issues/148)'s fix took 0277 ahead of 30, then again the day after that when [issue 130](https://github.com/datagen24/victual/issues/130) took 0280 ahead of 22, then again the same day when [issue 84](https://github.com/datagen24/victual/issues/84) (plan 19 piece 2) collided with issue 130's landed 0280 at merge time and moved to the lowest free slot instead, then again when [issue 176](https://github.com/datagen24/victual/issues/176)'s follow-up took 0282 ahead of both 22 and 32, then again when plan 32's own migration was written and, per `RESERVATIONS.md`'s same rule, took the lowest free slot (0283) below it. |
| 23 | [Storage classes](23-storage-classes.md) | Landed 2026-09-14 | `storage_classes` and `locations.storage_class_id`, migration `0274.pgsql.php`. `is_freezer` derived on write, not by trigger; `storage_class_id` carries a `FOREIGN KEY` the plan left open, unlike `parent_location_id`. See [Executed](23-storage-classes.md#executed). |
| 24 | [SQLite runtime retirement](24-sqlite-runtime-retirement.md) | Landed | ADR-0008's retirement work. The differential harness and migrations 0001–0255 were kept until 14 piece 2 landed (2026-09-17); their own retirement is unblocked but unscheduled. |
| 25 | [Label infrastructure](25-label-infrastructure.md) | Groups A, B and C implemented; physically accepted 2026-09-09; one verification open | Wave 3b, [issue 93](https://github.com/datagen24/victual/issues/93). Owns ADR-0011's machinery and answers ADR-0019's question 1; narrowed 2026-09-07 by [ADR-0021](../adr/0021-label-templates-are-application-data.md) so that templates, rendering, previews and artifacts are [27](27-label-templates-and-rendering.md)'s and this plan keeps identity (0269), jobs, printer configuration and the worker API (0270), the delivery worker and the import refusal. Both gating records were accepted 2026-09-07. **Verification 13 — a label requested in Victual, rendered, verified, claimed, printed on the QL-820NWBc and scanned back — was met 2026-09-09**, with 14 and 15 and the crash-after-send and renderer-independent-reprint cases; two-colour printed through the whole path the same day. **Open: verification 12's deployment half** — a K3S apply, or a container runtime whose egress reaches the printer; the label printed from the native build of the pinned worker because the local podman VM cannot open TCP to it. That is the same work as [20](20-container-infrastructure.md) piece 4 ([issue 133](https://github.com/datagen24/victual/issues/133)). Deferred and unscheduled: ADR-0019 item 7 steps 2–3 — migrating the five entity types that print through the webhook, which needs a wire-contract record, then deleting the webhook. |
| 26 | [Documentation site](26-documentation-site.md) | **Landed 2026-09-15** | Wave-independent. Piece 1, the developer section, is built: staging script, MkDocs and Read the Docs configuration, the pinned phpDocumentor reference, and a strict build in the `lint` job. Implements [ADR-0020](../adr/0020-documentation-publication-boundary.md), **Accepted 2026-09-14** with all four prerequisites met — piece 1 supplied the evidence prerequisites 2 and 4 asked for; its acceptance was bookkeeping, [issue 135](https://github.com/datagen24/victual/issues/135), closed separately from this plan. Piece 2, the manual, landed 2026-09-15, [issue 138](https://github.com/datagen24/victual/issues/138): `docs/usage.md` split into `docs/manual/`, a generated-and-checked configuration reference over the 85 settings `config-dist.php` now declares (84 when the issue was written), and task documentation over the 89 page routes `routes.php` now registers (81 when the issue was written — see this plan's [Executed](26-documentation-site.md#piece-2--the-manual-2026-09-15) section for the corrected counting method). |
| 27 | [Label templates and rendering](27-label-templates-and-rendering.md) | Implemented; no open follow-ups | Wave 3b, alongside 25. Owns the browser designer, the headless renderer, previews and print artifacts; 25 keeps identity, jobs, printer configuration and the delivery worker. Gated on [ADR-0021](../adr/0021-label-templates-are-application-data.md), accepted 2026-09-07 — **gate cleared**. Migrations 0271 and 0272 are in `master`; the designer landed in PR 113 (2026-09-09), and verification 4's physical print and scan-back were met the same day. Both remaining follow-ups **landed 2026-09-15**: the designer's fabric 5.x migration, which could not be a dependency bump ([issue 126](https://github.com/datagen24/victual/issues/126)) — see its Executed section for the origin-default regression the move found and fixed, which a dependency bump alone would have shipped invisibly — and sweep finding S32, the fail-closed group-to-read-permission table for the files API ([issue 136](https://github.com/datagen24/victual/issues/136)). Question 9, where red belongs, waits on a `stock_entry` field kind behind ADR-0019 item 7 step 2. |
| 28 | [Open container measurement](28-open-container-measurement.md) | **Landed 2026-09-14**, [issue 118](https://github.com/datagen24/victual/issues/118) | Wave 4. Implements [ADR-0022](../adr/0022-open-containers-carry-a-measured-remainder.md). Migration `0275.pgsql.sql`: the four `opened_*` columns on `stock`/`stock_log`, the coherence `CHECK`, `stock_current.amount_measured`, and `stock_next_use`/`uihelper_stock_entries` rebuilt to carry the new columns through — see [Executed](28-open-container-measurement.md#executed) for three view/trigger porting defects a live PostgreSQL run found that design review could not. `OpenProduct()`'s tare-enabled refusal is gone and it gains an optional `$measurement` parameter; a new `MeasureStockEntry()` re-measures an open single-unit entry; `AddProduct()`/`ConsumeProduct()`/`InventoryProduct()` lose the product-total tare arithmetic ADR-0022 decision 7 retires (`TransferProduct()`'s own refusal is [29](29-working-container-replenishment.md)'s, untouched here). `POST /stock/entry/{id}/measure` and an optional `measurement` object on `POST /stock/products/{id}/open`. UI: a scale button on `views/stockentries.blade.php` offered wherever coherence permits a measurement (no product-level "is this measured" configuration was built — question 5 is answered by universal offering, not curation). Verified against real PostgreSQL 16.13 (`openmeasure`, `views`, `triggers`, `rollback` and five other suite phases, all passing) and a real demo instance (`.devtools/frontend/open-container-measurement.js`, wired into the `frontend-security` job's actual invocation list). |
| 29 | [Working container replenishment](29-working-container-replenishment.md) | Landed | Wave 4, [issue 131](https://github.com/datagen24/victual/issues/131). Migration `0276.pgsql.sql`: the (product, location) minimum table and its `product_location_missing` shortfall view, `locations.tare_weight`/`tare_qu_id` (ADR-0022 decision 4), three one-tap refill columns on `products`, and a widened `uihelper_stock_current_overview`. `TransferProduct()`'s tare refusal is gone (this plan's half of ADR-0022 decision 7's retirement; plan 28 owns `OpenProduct()`'s) and `StockService::WeighLocation()` applies the location-scoped tared correction, exposed at `POST /stock/locations/{id}/weigh` and a by-label variant. The stock overview's location shortfall list is a one-tap refill action, never a filter — a location minimum is a refill prompt, never a shopping-list entry, which `AddMissingProductsToShoppingList()` stays untouched by. See [Executed](29-working-container-replenishment.md#executed) for the division with plan 28, the negative control against the retired product-scoped tare, and a pre-existing unrelated form defect found and routed around ([issue 159](https://github.com/datagen24/victual/issues/159)). Not built: a dedicated form for the location minimum itself (API-only for now) and question 2's MQTT publication of the shortfall. |
| 30 | [Nested product groups](30-nested-product-groups.md) | **Landed 2026-09-15**, [issue 124](https://github.com/datagen24/victual/issues/124) | Wave 4, first of the two product plans; ADR-0023 accepted 2026-09-14 and 07 retired the same day. Migration `0278.pgsql.sql`: `product_groups.parent_product_group_id`, `UNIQUE(parent_product_group_id, name) NULLS NOT DISTINCT` replacing the global name constraint, `product_groups_resolved`, and the nesting/delete guards copied from [08](08-nested-locations.md) — the advisory lock before the read and the VOLATILE guard function both carry over. **Second consumer of `hierarchy_depth_limit()`**, unchanged. API: `product_groups_resolved` in the `ExposedEntity*` enums and a `ProductGroupResolved` schema (`product_groups` itself has no OpenAPI schema to widen — none existed before this plan either); a delete pre-check beside the existing `locations` one in `GenericEntityApiController::DeleteObject()`. UI: the group form's parent picker and the product form's group dropdown show the path; filter-only group selects are unchanged, per question 2 below. See [Executed](30-nested-product-groups.md#executed) for the mixed-node demonstration (ADR-0023 decision 6), the two test-authoring defects the new suite phase's first run caught, and the one real defect [PR #165](https://github.com/datagen24/victual/pull/165)'s CI run found in the browser probe (a speculative payload-rendering check, written without a way to run it locally, duplicating `s29-payload.js`'s own coverage — removed rather than debugged blind). Owns [ADR-0023](../adr/0023-taxonomy-is-groups-packaging-is-parent-product.md)'s decision 1; questions 1–3 (minimum roll-up, shopping list grouping, whether `product_group_id` stays nullable) are unanswered and left for a follow-on. |
| 31 | [Directed substitution](31-directed-substitution.md) | **Landed 2026-09-15**, [issue 125](https://github.com/datagen24/victual/issues/125) | Wave 4, second of the two product plans; ADR-0023 accepted 2026-09-14. Beans substitute for grounds and never the reverse. Needed because 07-Q6 made those forms **separate products**, and today substitution is only a view over parentage — with no shared parent there is no relation at all. Migration `0279.pgsql.sql`: `product_substitutions` (a bare directed-edge table, self-edge and duplicate-pair refused) and `product_substitutions_resolved`, unioning it with `products_resolved` so the existing parent/child mechanism is one source among two rather than rewritten. Owns ADR-0023 decision 4. API: the edge entity plus the read-only resolved view in the three `ExposedEntity*` enums and `EntityReadPolicy`, and a `substitution_candidates` list on `GetProductDetails()`'s response. UI: a product-form section listing a product's edges with add/delete, mirroring the barcodes section. Landed ahead of [14](14-contract-and-regression-scaffolding.md) piece 2's freeze, as planned. See [Executed](31-directed-substitution.md#executed) for the four open questions' answers (no quantity factor, no transitive closure, no minimum-stock participation, no recipe-fulfilment wiring — the last three explicitly deferred rather than silently skipped) and why `products_current_substitutions` itself is untouched. |
| 32 | [Label kinds](32-label-kinds.md) | **Landed 2026-09-16**, [issue 182](https://github.com/datagen24/victual/issues/182) | Wave-independent. ADR-0019 decision item 7's steps 2 and 3: the five entity types that printed a Grocycode through the webhook — products, stock entries, recipes, chores, batteries — joined the label subsystem, and the webhook went with its four settings and `FEATURE_FLAG_LABEL_PRINTER`. Migration `0283.pgsql.php` (renumbered down from `0285.pgsql.php` after CI's migration-numbering check refused the un-renumbered branch — see [Executed](32-label-kinds.md#executed)): the three `CHECK`s widened to six kinds, `import_epoch` on the five target tables, one retirement trigger per table, five seeded QR-only default templates (see [Executed](32-label-kinds.md#executed) for why not the text line the plan asked for). Should land before [14](14-contract-and-regression-scaffolding.md) piece 2 snapshots, or 14 regenerates when it does. |

## Order of operations

The existing sequence is retained below. Each change must be independently mergeable and
meet its plan's verification criteria. Claim migration numbers in
[RESERVATIONS.md](../../migrations/RESERVATIONS.md); lower-numbered migrations merge first.
Recommitted 2026-09-14: every item still in a wave is either **ready** — nothing gates it and
an issue tracks it — or names the single gate in front of it.

| Wave | Work | State on 2026-09-14 |
|---|---|---|
| 0–1 | Scaffolding, security hotfix, platform work | Complete. Details are in the owning plans' Executed sections and the security sweep. |
| 2 | API correctness and authentication | Complete, 2026-09-04. API key expiry and rotation, sweep S11's remaining half, landed 2026-09-15 — [issue 130](https://github.com/datagen24/victual/issues/130). Q5's schema-derived allowlist waits on 14 piece 2. |
| 2.5 | [24](24-sqlite-runtime-retirement.md): SQLite runtime retirement under ADR-0008 | Complete, 2026-09-05. `DB_DRIVER` accepts `pgsql` alone, the SQLite migration line is frozen at 0265, and fixture-tested imports run from 0255 through it. The differential harness and migrations 0001–0255 were kept until 14 piece 2 landed (2026-09-17); retiring either is unblocked but unscheduled. |
| 3a | 19 piece 1: roles and read gating | Implemented, 2026-09-05; [ADR-0018](../adr/0018-role-grants-and-domain-reads.md), which records the model, accepted 2026-09-14. Price visibility is piece 2, wave 5. |
| 3b | 03 category minimums; 25 label infrastructure and 27 templates/rendering, then 06 location labels; optionally 09 | **Delivered.** 03 complete 2026-09-06. The location-label path — request, physical print, authorized scan back, demonstrated failure and reprint — was accepted on the QL-820NWBc 2026-09-09 and [issue 79](https://github.com/datagen24/victual/issues/79) closed on it; the print actions, the designer (PR 113) and the scan surface (PR 108) are in `master`, and nothing extends the webhook or emits a Grocycode. 27's designer off fabric 5.x, 06 Q5's tree path on the label, and sweep S32 all landed 2026-09-15, [issue 126](https://github.com/datagen24/victual/issues/126), [issue 137](https://github.com/datagen24/victual/issues/137) and [issue 136](https://github.com/datagen24/victual/issues/136). What the wave still owes: 25's verification 12 deployment half, [issue 93](https://github.com/datagen24/victual/issues/93), which is [20](20-container-infrastructure.md) piece 4 seen from the other side and ready. 09 stays deferred behind its experiment and S14, [issue 80](https://github.com/datagen24/victual/issues/80), both of which can run now. |
| 4 | 08 nested locations; 23 storage classes; 28 and 29; 30 and 31, the product hierarchy 07-Q6 chose | **Complete, 2026-09-15.** [08](08-nested-locations.md) 2026-09-09 (0273); [23](23-storage-classes.md) 2026-09-14 (0274); [28](28-open-container-measurement.md) 2026-09-14 (0275); [29](29-working-container-replenishment.md) 2026-09-14 (0276), its weighing half on the location tare ADR-0022 decision 4 carries; [issue 148](https://github.com/datagen24/victual/issues/148)'s nesting-trigger fix 2026-09-15 (0277); [30](30-nested-product-groups.md) 2026-09-15 (0278); [31](31-directed-substitution.md) 2026-09-15 (0279). ADR-0023 was accepted 2026-09-14 and [07](07-nested-products.md) retired the same day. Each plan's Executed section records what shipped and where it diverged. Follow-ups the wave left, none scheduled: plan 30's Q1 (does a group minimum roll up, touching 03's `product_groups_missing`) and Q2 (do lists group by path), plan 31's consume-screen and shopping-list suggestion surfaces, and plan 28's write-path findings recorded in its Executed section. |
| 5 | 14 piece 2 and 19 piece 2; then 02 read-only MCP and 05 A/C | 19 piece 2 landed 2026-09-15 (0281, [issue 84](https://github.com/datagen24/victual/issues/84) closed). **14 piece 2 landed 2026-09-17** ([issue 83](https://github.com/datagen24/victual/issues/83) closed) as the largest remaining item in the wave — the response-contract snapshot, `tests/Pgsql/ContractTest.php`, see [14's Executed section](14-contract-and-regression-scaffolding.md#executed) for the four comparison legs, the two real defects it found and fixed on the way (`CalendarApiController::Ical`), and what it does not close (S15, and S16's remaining half). 02, [issue 86](https://github.com/datagen24/victual/issues/86), and 05 A/C, [issue 85](https://github.com/datagen24/victual/issues/85), follow it; 05 needs a migration number claimed first. MCP uses the calling user's permissions. |
| — | Wave-independent work | Ready now: [20](20-container-infrastructure.md) pieces 2, 4, 5 and the credential split, [issue 133](https://github.com/datagen24/victual/issues/133); 18's three Home Assistant checks, [issue 139](https://github.com/datagen24/victual/issues/139), which need a running Home Assistant; of the review follow-ups from 2026-09-15's merges, [issue 177](https://github.com/datagen24/victual/issues/177) (files API own-picture exception, PR 187) and [issue 178](https://github.com/datagen24/victual/issues/178) (manual corrections, PR 188) closed by 2026-09-17; [issue 179](https://github.com/datagen24/victual/issues/179) (fabric 7 leftovers) closed 2026-09-17 by PR 190 after PR 172's second round had fixed its two named items. [Issue 176](https://github.com/datagen24/victual/issues/176), a fourth 2026-09-15 follow-up, closed the same day as migration 0282 and is off this list; note that its fix corrected one Manual statement issue 178 does not list (the roles page called price visibility “still-unbuilt” on the day it shipped), so read 178 against the tree rather than as a complete list. [26](26-documentation-site.md) landed 2026-09-15 and ADR-0020's acceptance closed [issue 135](https://github.com/datagen24/victual/issues/135) on 2026-09-14; both are off this list. [32](32-label-kinds.md) landed 2026-09-16 and closed [issue 182](https://github.com/datagen24/victual/issues/182); off this list too. **[15](15-deliberate-cleanup.md)'s remaining cleanup landed 2026-09-17 and closed [issue 132](https://github.com/datagen24/victual/issues/132)**, unblocked the same way once [issue 130](https://github.com/datagen24/victual/issues/130) and [issue 121](https://github.com/datagen24/victual/issues/121) closed 2026-09-15; off this list too. |

New migrations are PostgreSQL-only: the SQLite line is frozen at 0265 and
`check-migrations.php` refuses a `.sqlite.sql` above it. Migrations 0256–0265 keep the
dual-engine rules they were written under, and the differential harness in `.devtools/pgsql/`
still runs. 14 piece 2 landed 2026-09-17, which is what ADR-0008 named as the point the
harness's SQLite side may retire from — that retirement is unblocked now, not performed by
piece 2 itself, and is listed under Unscheduled work below.

Swift transport generation follows the API error contract and response snapshot. Swift UI
work follows 19 piece 2, which makes price fields optional. Home Assistant uses 18's MQTT
publication. See [17](17-ecosystem-clients.md) for client contracts and impact requirements.

Unscheduled work includes MCP writes after read-only use is proven, 05 B if shopping trips
justify it, 04's importer and datasets, plan 22 (23 landed 2026-09-14; 14 piece 2 landed
2026-09-17, so 22 is unblocked and simply not yet scheduled), and the two
retirements 24 deferred: archiving migrations 0001–0255, and the differential harness itself.
Both are unblocked now that 14 piece 2 has landed — neither has been done. The two label items that were deliberate follow-ups toward the
retirement ADR-0011 accepted — migrating the five entity types that printed through the
webhook, then deleting `VICTUAL_LABEL_PRINTER_WEBHOOK` with its constants — landed as
[32](32-label-kinds.md) on 2026-09-16. The wire-contract question that held them was
dissolved by [ADR-0024](../adr/0024-the-fork-writes-its-own-clients.md): the fork writes its
own clients, so the five `*/printlabel` endpoints were removed rather than reshaped. Observation
proposals (ADR-0012) remain accepted and unbuilt; that acceptance assigns no ownership and no
delivery slot. Interactive current-location scanning, which 06 deferred to a plan after 08, has
no plan yet.

**Label infrastructure is delivered, not merely owned.** [25](25-label-infrastructure.md)
implements ADR-0011 and ADR-0019; [27](27-label-templates-and-rendering.md) implements
ADR-0021, which superseded three boundaries of ADR-0011 — template ownership, "reprint is
resetting a row", and the importer's re-key obligation — so templates are application data, a
reprint replays retained artifact bytes, and an import refuses a target holding live labels.
Both records were accepted 2026-09-07, 0021 first because 0019's ownership model is the one
0021 decides. Schema (0269–0272), routes, the designer, the renderer and worker repositories
pinned into the flake, and the deployment manifests exist; a label printed and scanned back on
2026-09-09. Issue 79 closed on that evidence. Deployment under K3S is the one verification
still open, in issue 93.

**Plan 26 is independent of the wave order**, since it touches no runtime code. Both pieces
are now implemented: piece 1, the developer section of the documentation site, and piece 2,
the manual, covering the task documentation that did not exist anywhere in the repository or
upstream before it. It implements
[ADR-0020](../adr/0020-documentation-publication-boundary.md), **Accepted 2026-09-14** — piece
1 was built first deliberately, because two of that record's four acceptance prerequisites
asked for evidence only a working build could supply, and piece 1 supplied it.

## Hardening

Use these sources for findings, evidence, and verification details:

- [Architecture review](../architecture-review.md): original defects and hardening work.
- [Security sweep](../security-sweep.md): findings by S-number and remediation history.
- [Architecture rigor review](../architecture-rigor-review.md): documentation and design gaps.
- [Parity suite](../../.devtools/parity/README.md): manual comparison with upstream grocy.
- [Database suite](../../db/pgsql/README.md): engine, view, trigger, and migration checks.

The security sweep's routing is retained here. Partial findings remain open for the work
named in the last column.

| Findings | State | Owner or next work |
|---|---|---|
| S1–S4, S7, S23, S28 | Fixed | Security hotfix. |
| S5, S6, S8, S9, S12, S17–S19, S21, S27 | Fixed | Wave 2, plans 11/15. |
| S10 | Fixed | 01. |
| S25 | Fixed | 10. |
| S29 | Fixed | 12 and 21; `frontend-security` runs the payload probe on pull requests. |
| S11 | Fixed | 11: expiry and rotation landed 2026-09-15, [issue 130](https://github.com/datagen24/victual/issues/130). 02 must not restore query-string API keys. |
| S16 | Partial | 11 Q5: body-schema validation remains. Not closed by 14 piece 2, which landed 2026-09-17 without it — see that piece's [Executed](14-contract-and-regression-scaffolding.md#executed) entry for why it was judged separate, still-open work. |
| S13 | Fixed | `update.sh` and `.devtools/create_release_package.bat` deleted, 15-C11. |
| S32 | Fixed | `FilesApiController::GROUP_READ_PERMISSIONS`, a fail-closed group-to-read-permission table for the files API, [issue 136](https://github.com/datagen24/victual/issues/136). Was closed for the label groups by keeping them out of the `FileGroups` enum; the general finding is now closed too. |
| S14 | Open | 09: barcode filenames, image extensions, and fetch destination restrictions, [issue 80](https://github.com/datagen24/victual/issues/80). Required before any new source, so it can precede the experiment. |
| S15 | Open | Regex filter bounds. Not closed by 14 piece 2 (landed 2026-09-17) — separately-scoped input-validation work, unscheduled. |
| S20, S22, S24, S26 | Open | See sweep; unscheduled. |
| S30, S31 | Fixed | 19: piece 1 gated the six domain reads (2026-09-05), piece 2 gated prices (2026-09-15, [issue 84](https://github.com/datagen24/victual/issues/84)) and its four remaining channels closed the same day ([issue 176](https://github.com/datagen24/victual/issues/176), migration 0282). S31's own point — that the feature flag is not a permission — is what `User::PricesVisible()` now carries. |
| R1 | Fixed, regression check landed 2026-09-17 | 14 piece 2: `tests/Pgsql/ContractTest.php::testSystemConfigRetainsFeatureFlagStock` asserts `/system/config` retains `FEATURE_FLAG_STOCK`. |

Initial parity findings #44–#48 are fixed or recorded as accepted differences. The parity
suite's accepted-difference records hold the comparison details; it is a manual tool,
not a CI gate.

## Plan conventions

Follow [documentation conventions](../documentation.md). Keep numbered **Open questions**
and their inline `> **Response:**` answers. Preserve a landed plan's proposed design in
its original tense and add delivery details under **Executed**. Include client impact,
verification criteria, and dated, reproducible measurements. Cite code symbols rather
than unstable line numbers.

Architectural choices belong in [ADRs](../adr/README.md). Accepted ADRs constrain plans;
Proposed ADRs must be identified as proposals. Update this table and the work order
together when status or dependencies change.
